# Organik Food BD — Final Firebase product repair

The storefront reads the `products` collection from the existing Firebase project `organik-food-bd`.
It uses a server read plus a REST fallback and a realtime listener. It does not seed, delete, or replace existing Firebase products.

Deploy this project to the existing site/project; no Firebase data migration is required.
